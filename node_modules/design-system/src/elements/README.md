Elements aka Foundation – styling for bare HTML elements (like h1, a, buttons, etc.). These come with default styling from the browser so we can redefine them here (fonts, grid, icons, layout, typography, etc...).
