#!/usr/bin/env sh

git clone --single-branch --branch ${BRANCH_NAME} ${REPO_URL} /var/${RELEASE_DIR}

cd /var/${RELEASE_DIR}

npm install @semantic-release/git @semantic-release/changelog -D
npm run release
