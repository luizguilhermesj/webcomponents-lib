each group in locals.sources.icons
    h3= group.title
    div.columns
      each icon in group.icons
        span(class="columns-3 badge paragraph-title icon icon-" + icon.name)
          span.name= icon.name
          svg.inline-icon.inline-icon--md
            use(xlink:href="#" + icon.name)
