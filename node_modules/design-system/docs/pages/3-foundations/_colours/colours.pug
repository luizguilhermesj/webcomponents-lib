each colors_group in locals.sources.colours
    h2= colors_group.title
    ul.colors-box
      each color in colors_group.colors
        li.color
          span(style={ background: color.code }).color-tag
          h5= color.name
          span.code= color.code
