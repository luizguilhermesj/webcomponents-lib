each type in locals.sources['message-types']
    - var cardType= `message-card--secondary`
    div(class= `message-card ${cardType} ${cardType}--${type.name}`)
        div.message-card__contents
            div.message-card__content
                h5.inline-title= type.name
                p.paragraph Please Note: our returns policy has changed and you now have 30 days to return or exchange your item.
                code message-card #{cardType} #{cardType}--#{type.name}
