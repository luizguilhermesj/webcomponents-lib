h4.paragraph-title A. Primary
div.columns
    each type in locals.sources['message-types']
        - var cardType = `message-card`
        div(class= `${cardType} ${cardType}--${type.name}`)
            div.message-card__icon
                svg(class= `inline-icon inline-icon--inline inline-icon--${type.name}`)
                    use(xlink:href= "#information")

            div.message-card__content
                h4.paragraph-title= type.name
                p.paragraph Please Note: our returns policy has changed.



h4.paragraph-title B. Secondary (wider width)
div
    each type in locals.sources['message-types']
        - var cardType = `message-card--secondary`
            div(class= `message-card ${cardType} ${cardType}--${type.name}`)
                div.message-card__contents
                  div.message-card__icon
                      svg(class= `inline-icon inline-icon--inline inline-icon--${type.name}`)
                          use(xlink:href= "#information")

                  div.message-card__content
                      h4.paragraph-title= type.name
                      p.paragraph Please Note: our returns policy has changed and you now have 30 days to return or exchange your item. Please Note: our returns policy has changed and you now have 30 days to return or exchange your item.
