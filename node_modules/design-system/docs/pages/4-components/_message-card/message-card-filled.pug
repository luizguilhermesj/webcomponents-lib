div.columns
    div.message-card.message-card--filled--primary
        div.message-card__content
            h5.inline-title primary
            p.paragraph Note: Add contents
    div.message-card.message-card--filled--primary
        div.message-card__icon
            svg.inline-icon.inline-icon--inline
                use(xlink:href= "#information")

        div.message-card__content
            h4.paragraph-title primary
            p.paragraph Note: Add contents.

    each type in locals.sources['message-types']
        - var cardType = `message-card`
        div(class= `${cardType} ${cardType}--filled--${type.name}`)
            div.message-card__content
                h5.inline-title= type.name
                p.paragraph Note: Add contents
        div(class= `${cardType} ${cardType}--filled--${type.name}` )
            div.message-card__icon
                svg.inline-icon.inline-icon--inline
                    use(xlink:href= "#information")

            div.message-card__content
                h4.paragraph-title= type.name
                p.paragraph Note: Add contents.
